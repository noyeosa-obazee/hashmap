# hashmap

A custom hash map implementation in JavaScript with collision handling.

Features:

- Collision Resolution - Uses chaining (linked lists) to handle hash collisions

- Dynamic Resizing - Automatically expands when load factor exceeds 0.75

- Full API - Set, get, remove, check, and iterate through key-value pairs

- Optimized - Prime number hashing and efficient memory management
